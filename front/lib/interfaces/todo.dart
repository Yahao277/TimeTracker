import 'package:flutter/material.dart';

class WorkInProgres extends StatelessWidget {
  const WorkInProgres({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Working on it'),),
    );
  }
}
